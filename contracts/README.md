## WatchfulEye Contracts

This directory contains **versioned, machine-readable** request/response schemas for WatchfulEye.

### Structure
- `contracts/v3/*`: V3 API contracts (`/api/v3/*`)

### Rule
- If you change a contract, you must:
  - update the corresponding implementation
  - update/add tests proving the contract surface


